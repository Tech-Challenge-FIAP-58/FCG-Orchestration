apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-api
  template:
    metadata:
      labels:
        app: payment-api
    spec:

      initContainers:
        - name: wait-for-sqlserver
          image: busybox
          command:
            - sh
            - -c
            - |
              echo "Aguardando SQL Server..."
              until nc -z sqlserver 1433; do
                echo "SQL Server ainda não disponível..."
                sleep 5
              done
              echo "SQL Server disponível!"

        - name: wait-for-rabbitmq
          image: busybox
          command:
            - sh
            - -c
            - |
              echo "Aguardando RabbitMQ..."
              until nc -z rabbitmq 5672; do
                echo "RabbitMQ ainda não disponível..."
                sleep 5
              done
              echo "RabbitMQ disponível!"

      containers:
        - name: payment-api
          image: payment-api:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080

          envFrom:
            - configMapRef:
                name: payment-api-config

          env:
            - name: ConnectionStrings__Core
              valueFrom:
                secretKeyRef:
                  name: payment-api-secrets
                  key: connectionString

            - name: RabbitMQ__Username
              valueFrom:
                secretKeyRef:
                  name: payment-api-secrets
                  key: rabbitMQDefaultUser

            - name: RabbitMQ__Password
              valueFrom:
                secretKeyRef:
                  name: payment-api-secrets
                  key: rabbitMQDefaultPass

            - name: RabbitMQ__VirtualHost
              valueFrom:
                secretKeyRef:
                  name: payment-api-secrets
                  key: rabbitMQDefaultVHost

            - name: RabbitMQ__Host
              valueFrom:
                secretKeyRef:
                  name: payment-api-secrets
                  key: rabbitMQDefaultHost

            - name: PaymentConfig__DefaultApiKey
              valueFrom:
                secretKeyRef:
                  name: payment-api-secrets
                  key: paymentDefaultApiKey

            - name: PaymentConfig__DefaultEncryptionKey
              valueFrom:
                secretKeyRef:
                  name: payment-api-secrets
                  key: paymentDefaultEncryptionKey

          readinessProbe:
            exec:
              command:
                - sh
                - -c
                - |
                  test -e /proc/1
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 3

          livenessProbe:
            exec:
              command:
                - sh
                - -c
                - |
                  test -e /proc/1
            initialDelaySeconds: 30
            periodSeconds: 20
            failureThreshold: 3

          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "300m"
              memory: "256Mi"
