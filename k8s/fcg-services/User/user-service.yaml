apiVersion: v1
kind: Service
metadata:
  name: user-api
  namespace: fcg-services
spec:
  selector:
    app: user-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
