apiVersion: v1
kind: Service
metadata:
  labels:
    app: catalog-api
  name: catalog-api
  namespace: fcg-services
spec:
  selector:
    app: catalog-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
