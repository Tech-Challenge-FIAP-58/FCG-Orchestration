apiVersion: v1
kind: Service
metadata:
  name: payment-api
  namespace: fcg-services
spec:
  selector:
    app: payment-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
